{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch file",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/internal/testcmd/main.go",
            "args": [
                "-a=CLEAN",
                "-c=../../test_conf.yml"
            ]
        }
    ]
}